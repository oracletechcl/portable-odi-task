interface:
  display_name: "ODI Builder"
  short_description: "Migrate Pentaho to OCI DI with validated runnable mocks"
  default_prompt: "Use $odi-builder to migrate this Pentaho flow to OCI Data Integration, decide whether mocks are required, and validate or create every required mock."
